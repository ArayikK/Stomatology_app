import 'package:flutter/material.dart';

/// The one color the whole app's palette is generated from. Screens that
/// need colors outside the ambient theme (e.g. the always-dark tour overlay)
/// derive them from this seed too, so everything stays one family.
const Color kStomSeedColor = Colors.teal;
